package it.hendorsoftware.medishelf

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Test unitario minimale generato con lo scaffold.
 *
 * Verifica che l'ambiente JUnit locale sia correttamente configurato.
 */
class ExampleUnitTest {

    /**
     * Conferma l'esecuzione dei test JVM locali.
     */
    @Test
    fun additionIsCorrect() {
        assertEquals(4, 2 + 2)
    }
}
